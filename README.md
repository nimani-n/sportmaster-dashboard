# Sportmaster Dashboard

Interactive analytics dashboard built with Python, Streamlit, Pandas and Plotly.

## How to run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

Upload a CSV file with columns: `site_version`, `time`, `title`, `target`.
